
export interface LearningPath {
  id: string;
  title: string;
  index: string;
  description: string;
  thumbnail: string;
}

export interface ThemeConfig {
  accentColor: string;
  communityName: string;
  tagline: string;
  intro: string;
}
